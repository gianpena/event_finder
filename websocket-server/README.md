# How to build

1. Ensure that a `.env` file exists in this directory with `MYSQL_PASSWORD` defined.
2. If you haven't already, please install docker.
3. Run `docker compose up -d` (or leave out `-d` if you wish to run the command interactively).
